[target.aarch64-unknown-linux-gnu]
pre-build = [
    "dpkg --add-architecture $CROSS_DEB_ARCH",
    "apt-get update",
    "apt-get install --assume-yes libgtk-3-dev:$CROSS_DEB_ARCH libx11-dev:$CROSS_DEB_ARCH libxtst-dev:$CROSS_DEB_ARCH libxdo-dev:$CROSS_DEB_ARCH",
]
